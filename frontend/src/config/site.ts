/**
 * URL pública del sitio en producción.
 *
 * TODO: al publicar en dominio propio, sustituir por la URL final
 * (ej. 'https://islamontanatravel.com') y eliminar `basePath`/`assetPrefix`
 * de next.config.js — dejarán de ser necesarios fuera de GitHub Pages.
 */
export const SITE_URL = 'https://sadie27.github.io/IslaMontanaWeb'

export const SITE_NAME = 'Islamontana Travel'

/**
 * Locale actual del sitio (formato BCP 47 / og:locale).
 *
 * TODO (i18n futuro): cuando se añada inglés, esto pasará a depender de la
 * ruta (/es, /en) en vez de ser una constante fija. La migración de rutas
 * es una tarea aparte — no tocar aquí sin coordinarla con esa migración.
 */
export const SITE_LOCALE = 'es_EC'
